import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.farasi.mikehila',
  appName: 'MiKehila',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
